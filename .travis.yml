sudo:required
services:
  - docker

before_install:
  - docker build -t shadab/docker-react-project -f Dockerfile.dev .


script:
  - docker run -e CI=true shadab/docker-react-project npm run test

language:
  generic
  